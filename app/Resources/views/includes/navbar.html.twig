<nav id="nav-header-sticky" class="navbar navbar-default  " role="navigation">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('homepage') }}">
                {% image 'images/logo.png' %}
                <img src="{{ asset_url }}" alt="Site Logo" width="55px" />
                {% endimage %}
                <h1><small>SynFoo</small></h1>
            </a>
            <button type="button" class="navbar-toggle"
                    data-toggle="collapse"
                    data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                {{ knp_menu_render('main') }}
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user"></i>{{ 'layout.logged_in_as'|trans({'%username%': app.user.username | title}, 'FOSUserBundle') }}<span class="caret"></span></a>
                        {{ knp_menu_render('user') }}
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('fos_user_security_login') }}"><i class="fa fa-sign-in"></i>{{ 'link_login'|trans({}, 'SonataUserBundle') }} / <i class="fa fa-edit"></i>{{ 'link_register'|trans({}, 'SonataUserBundle') }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>