{{ form_start(form) }}
    {{ form_widget(form) }}

	<div class="form-process-buttons">
	<input type="submit" value="{{ button_label|default("Create post") }}"
           class="{{ button_css|default("btn btn-lg btn-primary") }}" />
    {% if include_back_to_home_link is not defined or include_back_to_home_link == true %}
        <a href="{{ path('admin_post_index') }}" class="btn btn-lg btn-link">
            Back to the post list
        </a>
    {% endif %}

	</div>

{{ form_end(form) }}
